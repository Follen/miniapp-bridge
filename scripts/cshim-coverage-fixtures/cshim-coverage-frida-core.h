#ifndef FRIDA_CORE_H
#define FRIDA_CORE_H

/* Keep the include name used by the production source while substituting the
 * deterministic test double during coverage builds. */
#include "cshim-coverage-stubs.h"

#endif
